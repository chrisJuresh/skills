# acme-console

Next.js admin console. Work merges through `development`; `main` is the release branch and
is also the repository's default branch on GitHub.

## Agent skills

### Issue tracker
Issues live in GitHub Issues for `acme/console`. See `docs/agents/issue-tracker.md`.
